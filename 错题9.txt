关于浏览器渲染性能的表述下列说明错误的是（）
A:由于JS是一种单线程语句，并且UI渲染线程和JS执行线程是相互的，所有才会出现JS执行过长导致浏览器UI卡顿的问题
B:对于高CPU密集型的任务，还可以使用WebWorker单独执行JS，Worker里的JS和UI是线程隔离的，不会互相影响
C:一旦事件阻塞，就会导致用户的操作失去响应，同时正在进行的CSS动画也会出现停止，滚动条也无法滚动
D:对于高CPU密集型的任务，我们应该尽可能的使用分片式的异步分批执行，这样才能防止渲染卡顿和事件阻塞